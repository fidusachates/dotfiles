general {
    lock_cmd = pidof $HOME/.local/bin/seed-lockscreen || $HOME/.local/bin/seed-lockscreen
}

listener {
    timeout = 180
    on-timeout = loginctl lock-session
}

# listener {
#     timeout = 600
#     on-timeout = systemctl suspend
# }
